# Deprecated. Use: conductor-core/compose.yaml
# Build trading-node: docker compose -f conductor-core/compose.yaml --profile build build trading-node

services:
  _use_conductor_core:
    image: alpine:3.20
    profiles: ["never"]
    entrypoint:
      [
        "sh",
        "-c",
        "echo Use conductor-core/compose.yaml && exit 1",
      ]
