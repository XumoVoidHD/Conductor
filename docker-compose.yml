# Deprecated. Use: conductor-core/docker-compose.yml
# Build trading-node: docker compose -f conductor-core/docker-compose.yml --profile build build trading-node

services:
  _use_conductor_core:
    image: alpine:3.20
    profiles: ["never"]
    entrypoint:
      [
        "sh",
        "-c",
        "echo Use conductor-core/docker-compose.yml && exit 1",
      ]
