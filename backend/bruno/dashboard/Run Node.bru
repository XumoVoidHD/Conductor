meta {
  name: Run Node
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/dashboard/nodes/run
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "node_id": "tn-REPLACE_ME"
  }
}
