meta {
  name: Restart Node
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/dashboard/nodes/restart
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "node_id": "tn-REPLACE_ME"
  }
}

docs {
  Restart the trading node container/process. Strategy comes back STOPPED — call Run.
}
