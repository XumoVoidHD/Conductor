meta {
  name: Status Node
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/dashboard/nodes/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "node_id": "tn-REPLACE_ME"
  }
}
