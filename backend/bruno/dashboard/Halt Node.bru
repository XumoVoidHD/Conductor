meta {
  name: Halt Node
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/dashboard/nodes/halt
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "node_id": "tn-REPLACE_ME"
  }
}
