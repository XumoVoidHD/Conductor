meta {
  name: Me
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Current authenticated user. Uses Bearer `{{accessToken}}` from the Local environment (set by Login).
}
